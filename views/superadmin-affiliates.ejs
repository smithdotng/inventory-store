<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shed | Affiliates</title>
  <%- include('partials/app-head', { extraCssUrl: '' }) %>
</head>
<body>

<!-- Superadmin Sidebar -->
<aside class="app-sidebar" id="appSidebar">
  <div class="sidebar-logo"><img src="/images/logo.png" alt="Shed"></div>
  <nav class="sidebar-nav">
    <a href="/superadmin/dashboard"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
    <a href="/superadmin/create-admin"><i class="fas fa-user-plus"></i> Create Admin</a>
    <a href="/superadmin/affiliates" class="active"><i class="fas fa-handshake"></i> Affiliates</a>
    <a href="/superadmin/outlets"><i class="fas fa-store"></i> All Outlets</a>
    <a href="/superadmin/ads"><i class="fas fa-ad"></i> Featured Ads</a>
    <a href="/superadmin/sent-messages"><i class="fas fa-envelope"></i> Sent Messages</a>
  </nav>
  <div class="sidebar-bottom">
    <a href="/admin-logout"><i class="fas fa-power-off"></i> Log Out</a>
  </div>
</aside>
<div class="sidebar-overlay" id="sidebarOverlay"></div>

<div class="app-main">
  <header class="app-topbar">
    <div class="topbar-left">
      <button class="hamburger-btn" id="hamburgerBtn"><i class="fas fa-bars"></i></button>
      <span class="page-title">Affiliates</span>
    </div>
    <div class="topbar-right">
      <span class="topbar-business">Superadmin</span>
      <div class="user-dropdown" id="userDropdown">
        <button class="topbar-user-chip user-dropdown-btn" id="userDropdownBtn">
          <div class="avatar">S</div>
          Superadmin
          <i class="fas fa-chevron-down user-dropdown-chevron"></i>
        </button>
        <div class="user-dropdown-menu" id="userDropdownMenu">
          <a href="/admin-logout" class="dropdown-item-danger"><i class="fas fa-sign-out-alt"></i> Log Out</a>
        </div>
      </div>
    </div>
  </header>

  <main class="app-content">

    <% if (Array.isArray(success) && success.length > 0) { %>
      <% success.forEach(msg => { %><div class="flash-success"><i class="fas fa-check-circle"></i> <%= msg %></div><% }) %>
    <% } %>
    <% if (Array.isArray(error) && error.length > 0) { %>
      <% error.forEach(msg => { %><div class="flash-error"><i class="fas fa-exclamation-circle"></i> <%= msg %></div><% }) %>
    <% } %>

    <div class="page-header">
      <div><h1>Affiliates</h1><p>All registered referral affiliates</p></div>
    </div>

    <div class="app-card">
      <% if (affiliates && affiliates.length > 0) { %>
        <div style="overflow-x:auto;">
          <table class="app-table">
            <thead>
              <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Email</th>
                <th>Country</th>
                <th>Phone</th>
                <th>Referral Code</th>
                <th>Clicks</th>
                <th>Signups</th>
                <th>Joined</th>
              </tr>
            </thead>
            <tbody>
              <% affiliates.forEach(affiliate => { %>
                <tr>
                  <td><%= affiliate.firstName || 'N/A' %></td>
                  <td><%= affiliate.lastName || 'N/A' %></td>
                  <td><%= affiliate.email || 'N/A' %></td>
                  <td><%= affiliate.country || 'N/A' %></td>
                  <td><%= affiliate.countryCode %> <%= affiliate.mobileNumber || 'N/A' %></td>
                  <td><code style="background:#f0f0f0; padding:2px 6px; border-radius:4px;"><%= affiliate.referralCode || 'N/A' %></code></td>
                  <td style="font-weight:600;"><%= affiliate.clickCount %></td>
                  <td style="font-weight:600; color:#008B8B;"><%= affiliate.signupCount %></td>
                  <td style="font-size:0.83rem; color:#888;">
                    <%= affiliate.createdAt ? new Date(affiliate.createdAt).toLocaleDateString() : 'N/A' %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <div style="text-align:center; padding:80px; color:#999;">
          <i class="fas fa-handshake" style="font-size:3.5rem; color:#ddd; margin-bottom:20px; display:block;"></i>
          <h3 style="color:#0d0d1a; margin-bottom:8px;">No affiliates yet</h3>
          <p>Affiliates who register will appear here.</p>
        </div>
      <% } %>
    </div>

  </main>
</div>

<script src="/js/plugins/jquery.min.js"></script>
<script>
  document.getElementById('hamburgerBtn').addEventListener('click', function() {
    document.getElementById('appSidebar').classList.toggle('open');
    document.getElementById('sidebarOverlay').classList.toggle('active');
  });
  document.getElementById('sidebarOverlay').addEventListener('click', function() {
    document.getElementById('appSidebar').classList.remove('open');
    this.classList.remove('active');
  });
</script>
</body>
</html>
